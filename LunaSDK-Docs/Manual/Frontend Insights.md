Frontend Insights is a library for debugging a service that uses [[Frontend]] as API layer. When enabled, it captures all messages between server and client, so that they can be replayed later for analyzing. Frontend Insights can be embedded to the server as an additional layer, it can also be run as a standalone tool that relays messages between server and client.

