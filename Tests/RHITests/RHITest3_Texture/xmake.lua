target("RHITest3_Texture")
    add_luna_sdk_options()
    set_group("Tests/RHITest")
    set_kind("binary")
    add_headerfiles("*.hpp")
    add_files("*.cpp")
    add_deps("Runtime", "RHI", "RHITestBed", "ShaderCompiler", "Image")
    before_build(function(target)
        os.cp("$(scriptdir)/uv_checker.png", target:targetdir() .. "/uv_checker.png")
    end)
    after_install(function (target)
        os.cp(target:targetdir() .. "/uv_checker.png", target:installdir() .. "/bin/uv_checker.png")
    end)
target_end()